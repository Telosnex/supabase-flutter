\* RealtimeChannel — positive configuration.
\*
\* Post-audit: unsubscribe() collapses to ONE atomic step, so the
\* strict form of RejoinBookkeeping (rejoinScheduled ⇒ state ∈
\* {errored, joining}) now holds.  The prior counterexample config
\* RealtimeChannel_TimerLeakOnLeave.cfg has been retired — it was a
\* model artifact caused by splitting unsubscribe into two actions.
\*
\* Run:
\*   cd formal_models/realtime_channel
\*   java -XX:+UseParallelGC \
\*     -cp "/Applications/TLA+ Toolbox.app/Contents/Eclipse/tla2tools.jar" \
\*     tlc2.TLC RealtimeChannel.tla -config RealtimeChannel.cfg \
\*     -workers auto -deadlock
\*
\* Expected: all invariants and properties PASS.

SPECIFICATION Spec

CONSTANTS
    MaxPushes = 3

INVARIANTS
    TypeOK
    RejoinBookkeeping
    PushBufferOnlyAfterJoined
    ClosedIsStable

PROPERTIES
    ClosedIsTerminal
    JoinedOnceIsMonotone
    JoinOkFlushesBuffer
    JoinedClearsRejoinTimer
    UnsubscribeLeadsToClosed
