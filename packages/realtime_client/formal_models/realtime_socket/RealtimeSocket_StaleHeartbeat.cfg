\* RealtimeSocket — counterexample config for stale pendingHeartbeatRef
\* persisting across sessions.
\*
\* Property FreshOpenNoStalePendingHeartbeat EXPECTED TO FAIL.
\*
\* No lifecycle event clears pendingHeartbeatRef — only
\*   • onConnMessage when a reply ref matches (clears it), or
\*   • sendHeartbeat discovering a pending ref (nulls, then closes).
\* Crucially, _onConnOpen, _onConnClose, disconnect() and connect()
\* all leave the field untouched.
\*
\* Resulting trace (approximate):
\*   1. UserConnect           → Connecting
\*   2. ConnectReadySuccess   → Open, heartbeatTimerOn=TRUE
\*   3. HeartbeatSend         → pendingHeartbeat=TRUE
\*   4. ServerClose           → Closed; pendingHeartbeat STAYS TRUE
\*                              reconnectScheduled=TRUE, heartbeat off
\*   5. ReconnectTimerFires   → auto disconnect starts
\*   6. FinishDisconnect      → thisConn=0; pendingHeartbeat STAYS TRUE
\*   7. AutoConnect           → Connecting (fresh localConn)
\*   8. ConnectReadySuccess   → Open; pendingHeartbeat STILL TRUE ✗
\*
\* At step 8 the new session is Open with a stale pendingHeartbeatRef.
\* The very next HeartbeatTick will hit HeartbeatTimeout and close it.
\*
\* Source: realtime_client.dart:
\*   line 97   pendingHeartbeatRef declaration
\*   line 382–396  _onConnOpen (does not clear)
\*   line 399–413  _onConnClose (does not clear)
\*   line 266–299  disconnect (does not clear)
\*   line 347      onConnMessage clears only on ref match

SPECIFICATION Spec

CONSTANTS
    MaxAttempts             = 3
    MaxRef                  = 4
    AllowSyncTransportThrow = FALSE

PROPERTIES
    FreshOpenNoStalePendingHeartbeat
