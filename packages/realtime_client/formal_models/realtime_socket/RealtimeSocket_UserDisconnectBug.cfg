\* RealtimeSocket — counterexample config for Finding #1 (user path).
\*
\* Invariant NoReconnectAfterUserDisconnect EXPECTED TO FAIL.
\*
\* Shortest TLC trace:
\*   1. UserConnect         Connecting
\*   2. ConnectReadyError   Closed,  reconnectScheduled = TRUE
\*   3. UserDisconnect      oldState = Closed → shouldCloseSink = FALSE
\*                          userDisconnected = TRUE
\*   4. FinishDisconnect    thisConn = 0 but reconnectScheduled STILL TRUE
\*                          (reconnectTimer.reset() sits inside the
\*                          `if (shouldCloseSink)` branch at line 292)
\*
\* Source: realtime_client.dart:266–299 (disconnect).

SPECIFICATION Spec

CONSTANTS
    MaxAttempts             = 2
    MaxRef                  = 3
    AllowSyncTransportThrow = FALSE

INVARIANTS
    TypeOK
    NoReconnectAfterUserDisconnect
